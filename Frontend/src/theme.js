// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#42a5f5', // Custom primary color
    },
    secondary: {
      main: '#009FFF', // Custom secondary color
    },
  },
});

export default theme;
